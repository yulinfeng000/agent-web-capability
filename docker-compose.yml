services:
  agent-web-capability:
    container_name: awc-server
    build: .
    ports:
      - "8010:8010"
    environment:
      - MCP_MOUNT=1
      - MCP_CORS_ORIGINS=*
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    restart: unless-stopped
